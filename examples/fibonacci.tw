print("=== Fibonacci Sequence Generator ===");
print("Enter the term number (n):");
let n = int(input()) + 1;

print("=== Generating Sequence ===");
let a = 0;
let b = 1;

for (let i = 2; i < n; i = i + 1) {
    let next = a + b;
    a = b;
    b = next;
}

print(b);